export const calculateTotal = (price: number, amount: number) => price * amount;

export const calculateAmount = (total: number, price: number) => total / price;

export const calculatePrice = (total: number, amount: number) => total / amount;
